name: flutter_switch_app
version: 0.1.0
publish_to: 'none'

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  web_socket_channel: ^2.4.0

flutter:
  uses-material-design: true
